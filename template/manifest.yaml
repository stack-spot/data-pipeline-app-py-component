data-pipeline:
  region: us-east-1
  arn_bucket_source: arn:aws:s3:::328531315820-raw-datalake
  arn_bucket_target: arn:aws:s3:::328531315820-clean-datalake
  database:
    name: datalake
    schemas:
      path: ./template

